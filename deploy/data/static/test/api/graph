{"root":"openwiki","generatedAt":"2026-09-18T03:05:10.220Z","types":["Component","Domain Concept","Playbook","Quickstart","Section"],"nodes":[{"id":"build-and-validation","title":"构建与验证","type":"Playbook","description":"pom.xml 的构建模型、exec-maven-plugin 注入客户端 jute.maxbuffer 的作用范围与陷阱、按成本分层的验证命令，以及修改构建或运行方式时的检查清单。","tags":["maven","build","validation","jute-maxbuffer","jdk"],"body":"\n# 构建与验证\n\n## 构建模型\n\n| 项 | 取值 | 出处 |\n| --- | --- | --- |\n| 项目坐标 | `com.example:zk-repro:1.0-SNAPSHOT`，`packaging=jar` | `pom.xml` |\n| Java 目标 | `maven.compiler.source/target = 1.8` | `pom.xml` |\n| 依赖 | 仅 `org.apache.zookeeper:zookeeper:3.8.4` | `pom.xml` |\n| 构建插件 | `org.codehaus.mojo:exec-maven-plugin:3.6.2`，注入系统属性 `jute.maxbuffer=2097152` | `pom.xml` |\n| 测试 | 无测试依赖、无 surefire 配置、无 `src/test/` | `pom.xml`、`git ls-files` |\n| 产物 | `target/zk-repro-1.0-SNAPSHOT.jar`（由坐标推导；历史上该文件曾被提交入库，后随 `.gitignore` 增加 `target/` 一并移出版本控制） | `pom.xml`、`.gitignore` |\n\n主类名是 `ZkRepro`（`src/main/java/ZkRepro.java` 位于默认包），因此 `exec:java` 需要显式指定 `-Dexec.mainClass=ZkRepro`。该程序的行为本身见 [zk-repro-client.md](zk-repro-client.md)。\n\n## 客户端属性注入的范围（最容易踩的坑）\n\n`pom.xml` 把 `jute.maxbuffer` 配在 `exec-maven-plugin` 的 `<systemProperties>` 里，所以它**只作用于 `mvn exec:java` 启动的那个 JVM**：\n\n- 用 `mvn -q exec:java -Dexec.mainClass=ZkRepro` 运行 → 自动带上 `-Djute.maxbuffer=2097152`。\n- 用 `java -cp target/zk-repro-1.0-SNAPSHOT.jar …` 或 `java -jar` 直接运行 → **不会**带上该属性，必须自己加 `-Djute.maxbuffer=2097152`，否则即使服务端已调大也会写入失败（原因见 [jute-maxbuffer-limit.md](jute-maxbuffer-limit.md)）。\n- 需要 classpath 时，可以用解包后的 `apache-zookeeper-3.8.4-bin/lib/*.jar`（其中含 `zookeeper-3.8.4.jar` 与 `zookeeper-jute-3.8.4.jar`），见 [zookeeper-environment.md](zookeeper-environment.md)。\n\n## 命令\n\n| 目的 | 命令 | 说明 |\n| --- | --- | --- |\n| 构建（常规） | `mvn -q clean package` | `-q` 抑制成功日志、保留失败输出；静默成功即为通过 |\n| 运行演示（需要服务端） | `mvn -q exec:java -Dexec.mainClass=ZkRepro` | 唯一会自动带上客户端 `jute.maxbuffer` 的运行路径 |\n| 准备并启动服务端 | 见 [zookeeper-environment.md](zookeeper-environment.md) | 必须先启动，否则客户端永久阻塞 |\n| 只读核对服务端配置 | `tar -xzOf env/zookeeper_env.tar.gz apache-zookeeper-3.8.4-bin/conf/java.env` | 确认服务端侧取值 |\n| 无 Maven 时的编译检查 | `tar -zxf env/zookeeper_env.tar.gz && javac -d target/classes -cp \"apache-zookeeper-3.8.4-bin/lib/*\" src/main/java/ZkRepro.java` | 仅验证编译；**不会**带上 `jute.maxbuffer`，不能替代端到端验证 |\n\n## 验证分层\n\n- **常规（每次改动都应执行）**：`mvn -q clean package`。覆盖编译与打包，是 `src/main/java/ZkRepro.java`、`pom.xml` 任何改动的最小门槛。\n- **条件（仅当改动涉及上限、负载大小、连接目标或运行方式时才需要）**：解包并启动本地服务端，跑 `mvn -q exec:java -Dexec.mainClass=ZkRepro`，断言输出为 `Success! (Expected with fix)`；这套步骤同时覆盖服务端与客户端两侧的配置，任一侧缺失都会看到 `Caught unexpected exception: …ConnectionLossException…`。\n- **不存在可跑的测试套件**：仓库无测试目录、无测试依赖、无 CI 配置；因此没有\"跑一遍测试\"这一层。行为验证只有手工端到端。\n- **环境前提**：端到端步骤需要 JDK + Maven。本开发镜像中没有 `mvn`（`java -version` 为 openjdk 23，而 `pom.xml` 目标为 1.8），端到端验证须在装有 Maven 的环境执行。\n\n## 修改构建或运行方式时的检查清单\n\n1. 改 `exec-maven-plugin` 的 `systemProperties` → 服务端侧取值必须同步修改（[zookeeper-environment.md](zookeeper-environment.md)），否则端到端仍失败；只调一侧不算完成。\n2. 改 `maven.compiler.source/target` → 确认执行环境安装了对应 JDK；当前环境只有更高的 JDK，需实测编译结果（这也是 [quickstart.md](quickstart.md) Backlog 中的证据缺口）。\n3. 改 `artifactId` / `version` → 产物 jar 名变化，手工 `java -cp` 命令与文档中的路径都要同步。\n4. 引入测试框架 → 需要新增测试依赖与 surefire 配置，并先让失败可被观测（`ZkRepro` 当前吞掉异常、退出码恒为 0，见 [zk-repro-client.md](zk-repro-client.md) 配方 C）。\n5. 不要手工提交 `target/` 产物：`.gitignore` 已忽略该目录（契约说明见 [zookeeper-environment.md](zookeeper-environment.md)）。\n6. 升级 ZooKeeper 版本：`pom.xml` 的依赖版本与归档包内的客户端/服务端 jar（`lib/zookeeper-3.8.4.jar`、`lib/zookeeper-jute-3.8.4.jar`）当前同为 3.8.4，两处必须同步替换，否则客户端与服务端版本不一致（见 [zookeeper-environment.md](zookeeper-environment.md)）。\n","size":3191,"links":["zk-repro-client","jute-maxbuffer-limit","zookeeper-environment","quickstart"],"backlinks":["index","jute-maxbuffer-limit","quickstart","zk-repro-client","zookeeper-environment"]},{"id":"index","title":"Home","type":"Section","description":"","tags":[],"body":"\n# 文件\n\n- [构建与验证](build-and-validation.md) - pom.xml 的构建模型、exec-maven-plugin 注入客户端 jute.maxbuffer 的作用范围与陷阱、按成本分层的验证命令，以及修改构建或运行方式时的检查清单。\n- [jute.maxbuffer 与 1MB 数据包上限](jute-maxbuffer-limit.md) - ZooKeeper 默认 1MB 数据包上限的触发机制与 ConnectionLossException 表现，以及必须在服务端与客户端同时调大该上限的修复要求、单位与生效条件。\n- [快速上手](quickstart.md) - ZooKeeper 1MB 限制复现与修复仓库的知识库入口，包含仓库概览、页面导航、按变更意图的任务路由表、关键不变量与最小验证命令。\n- [ZkRepro 复现客户端](zk-repro-client.md) - src/main/java/ZkRepro.java 单类演示程序的职责、关键符号、连接与写入流程、阻塞与退出码等不变量陷阱，以及改动负载/目标集群/失败检测的变更配方。\n- [打包的 ZooKeeper 服务端环境](zookeeper-environment.md) - env/zookeeper_env.tar.gz 的内容清单、服务端 jute.maxbuffer 的生效链路、启动停止与数据目录/端口约定、.gitignore 契约，以及重新打包服务端配置的安全步骤。\n","size":659,"links":["build-and-validation","jute-maxbuffer-limit","quickstart","zk-repro-client","zookeeper-environment"],"backlinks":[]},{"id":"jute-maxbuffer-limit","title":"jute.maxbuffer 与 1MB 数据包上限","type":"Domain Concept","description":"ZooKeeper 默认 1MB 数据包上限的触发机制与 ConnectionLossException 表现，以及必须在服务端与客户端同时调大该上限的修复要求、单位与生效条件。","tags":["zookeeper","jute-maxbuffer","connection-loss","limits","configuration"],"body":"\n# jute.maxbuffer 与 1MB 数据包上限\n\n## 问题：默认 1MB 的硬上限\n\nZooKeeper 的序列化层（jute）对单个数据包有一个上限，默认在 1MB 量级——`README.md` 与 `src/main/java/ZkRepro.java` 的注释都把它写作 1MB / 1048576 字节。当客户端提交的序列化请求包超过该上限时，服务端会关闭连接，客户端侧表现为 `KeeperException$ConnectionLossException`。\n\n`README.md` 的 \"The Issue\" 一节把这条行为写成本仓库要复现的核心现象；修复前的 `README.md` 版本甚至把 `Caught expected exception: org.apache.zookeeper.KeeperException$ConnectionLossException: KeeperErrorCode = ConnectionLoss for /big_data_node` 记作预期输出（可用 `git log -p -- README.md` 查看该次改动），可以据此确认失败表现。\n\n本仓库的演示负载刻意只比上限多一点点：`src/main/java/ZkRepro.java` 中 `sizeInBytes = 1024 * 1024 + 100`，即 **1048676 字节**（`README.md` 的预期输出同样写的是 `Attempting to write 1048676 bytes`）。负载本身已经超过默认上限，再叠加 jute 的包长前缀与请求头，序列化后的包必然越界。\n\n```mermaid\nflowchart TD\n  A[\"客户端提交 1048676 字节负载\"] --> B[\"jute 序列化为请求包 含长度前缀与请求头\"]\n  B --> C{\"序列化包大小 是否超过 jute.maxbuffer\"}\n  C -->|\"未超过\"| D[\"服务端处理请求 返回创建成功\"]\n  C -->|\"超过\"| E[\"服务端关闭连接\"]\n  E --> F[\"客户端抛出 ConnectionLossException\"]\n  D --> G[\"ZkRepro 打印 Success 文本\"]\n```\n\n图示说明：上限判定发生在序列化包层面，因此\"刚好超过 1MB 的负载\"与\"远超 1MB 的负载\"表现相同。\n\n## 修复：两侧都必须设置\n\n`README.md` 明确要求：要支持更大的数据包，必须在**服务端和客户端**都设置 `jute.maxbuffer` 系统属性（单位：字节）。本仓库两侧都取 `2097152`（2MB）。\n\n| 侧 | 配置载体 | 生效方式 | 证据 |\n| --- | --- | --- | --- |\n| 服务端 | `env/zookeeper_env.tar.gz` 内 `apache-zookeeper-3.8.4-bin/conf/java.env`：`export JVMFLAGS=\"-Djute.maxbuffer=2097152\"` | 启动脚本 `bin/zkEnv.sh` 检测到 `conf/java.env` 后 source 它，`bin/zkServer.sh` 再把 `$JVMFLAGS` 传给服务端 JVM | 包内 `bin/zkEnv.sh`（`. \"$ZOOCFGDIR/java.env\"`）与 `bin/zkServer.sh`（`java … $JVMFLAGS $ZOOMAIN \"$ZOOCFG\"`） |\n| 客户端 | `pom.xml` 中 `exec-maven-plugin` 的 `<systemProperties>`：`jute.maxbuffer=2097152` | 只在 Maven 的 `exec:java` 目标上注入到被测 JVM | `pom.xml` 的 `<build>/<plugins>` |\n\n服务端配置的存放位置与重打包流程见 [zookeeper-environment.md](zookeeper-environment.md)；客户端注入的作用范围与陷阱见 [build-and-validation.md](build-and-validation.md)；把这条规则落地的程序实体是 [zk-repro-client.md](zk-repro-client.md)。\n\n## 外部可观察的不变量\n\n- **单位是字节**：`2097152` 表示 2MB，不是\"2 个单位\"；调大时要按目标包大小换算。\n- **两侧一致或服务端不小于客户端**：属性同时限制客户端发送与服务端接收/回送的包；只改一侧时超限的包仍会被拒绝。\n- **属性属于 JVM 系统属性，在进程启动时生效**：改完 `conf/java.env` 必须重启服务端；改完 `pom.xml` 必须重新以 `exec:java` 启动客户端。不能指望运行中的会话热更新。\n- **上限只是传输边界**：本仓库只演示\"能否传输\"，并不评估把大对象长期放在 znode 里的存储与内存开销；改动上限值时应自行评估服务端内存、快照体积与会话压力。\n\n## 如何验证上限已生效\n\n| 目的 | 命令 |\n| --- | --- |\n| 读取包内服务端配置 | `tar -xzOf env/zookeeper_env.tar.gz apache-zookeeper-3.8.4-bin/conf/java.env` |\n| 确认客户端侧配置存在 | 查看 `pom.xml` 中 `exec-maven-plugin` 的 `systemProperties` |\n| 行为验证（需要已启动的本地服务端） | `mvn -q exec:java -Dexec.mainClass=ZkRepro`，期望输出 `Success! (Expected with fix)` |\n\n行为验证无法用仓库内的自动化测试替代：仓库没有测试目录，也没有测试依赖。完整前置条件与验证分层见 [build-and-validation.md](build-and-validation.md)。\n","size":2627,"links":["zookeeper-environment","build-and-validation","zk-repro-client"],"backlinks":["build-and-validation","index","quickstart","zk-repro-client","zookeeper-environment"]},{"id":"quickstart","title":"快速上手","type":"Quickstart","description":"ZooKeeper 1MB 限制复现与修复仓库的知识库入口，包含仓库概览、页面导航、按变更意图的任务路由表、关键不变量与最小验证命令。","tags":["quickstart","navigation","zookeeper","jute-maxbuffer","maven"],"body":"\n# ZooKeeper 1MB 限制复现与修复 — 知识库\n\n## 这个仓库是什么\n\n一个**最小演示工程**，不是库也不是服务。它复现 ZooKeeper 默认 1MB 数据包上限（`jute.maxbuffer`）导致的 `ConnectionLossException`，并演示必须**服务端与客户端同时调大**该上限才能写入超过 1MB 的数据。\n\n| 交付物 | 路径 | 作用 |\n| --- | --- | --- |\n| 复现客户端 | `src/main/java/ZkRepro.java` | 单类、默认包；连接 `127.0.0.1:2181` 后向 `/big_data_node` 写入 1048676 字节 |\n| 构建与客户端配置 | `pom.xml` | ZooKeeper 3.8.4 依赖；`exec-maven-plugin` 注入客户端侧 `jute.maxbuffer=2097152` |\n| 预配置服务端 | `env/zookeeper_env.tar.gz` | Apache ZooKeeper 3.8.4 发行包，内含服务端侧 `jute.maxbuffer` 设置与 `zoo.cfg` |\n| 运行说明 | `README.md` | 人类可读的环境准备、启动、运行与预期输出 |\n| 版本控制契约 | `.gitignore` | 忽略解包目录、数据目录、构建产物；显式放行 `env/zookeeper_env.tar.gz` |\n\n仓库中**没有**：发布的 API、自动化测试、CI 配置、其它模块。改动点因此集中在 3 个文件加 1 个归档包上。\n\n## 知识库导航\n\n| 页面 | 何时阅读 |\n| --- | --- |\n| [jute-maxbuffer-limit.md](jute-maxbuffer-limit.md) | 想搞清楚\"为什么 1048676 字节会失败\"\"为什么两侧都要设置\" |\n| [zk-repro-client.md](zk-repro-client.md) | 要改复现客户端的行为、理解它的阻塞与退出码语义 |\n| [zookeeper-environment.md](zookeeper-environment.md) | 要解包/启动服务端、改服务端配置、理解 `.gitignore` 契约 |\n| [build-and-validation.md](build-and-validation.md) | 要改 `pom.xml`、选择运行方式、判断该跑哪些验证 |\n\n本仓库规模很小，因此知识库采用扁平结构，没有分节目录，也没有单独的 Source Map 页；每页在正文内直接内联源文件引用。\n\n## 任务路由表\n\n| 变更区域 / 意图 | 相关页面 | 精确源码入口 | 重要符号 | 聚焦测试 | 最小验证命令 |\n| --- | --- | --- | --- | --- | --- |\n| 改写入负载大小 | [zk-repro-client.md](zk-repro-client.md) | `src/main/java/ZkRepro.java` | `ZkRepro.main`、`sizeInBytes`、`bigData` | 无测试目录 | `mvn -q clean package`；服务端在运行时再跑 `mvn -q exec:java -Dexec.mainClass=ZkRepro` |\n| 改目标节点路径或 ACL/节点模式 | [zk-repro-client.md](zk-repro-client.md) | `src/main/java/ZkRepro.java` | `nodePath`、`zk.create`、`CreateMode.PERSISTENT`、`ZooDefs.Ids.OPEN_ACL_UNSAFE` | 无测试目录 | 同上 |\n| 改连接地址或会话超时 | [zk-repro-client.md](zk-repro-client.md)、[zookeeper-environment.md](zookeeper-environment.md) | `src/main/java/ZkRepro.java` | `new ZooKeeper(\"127.0.0.1:2181\", 3000, …)`、`connectedSignal` | 无测试目录 | 服务端 `bin/zkServer.sh status` 确认端口后跑 `mvn -q exec:java -Dexec.mainClass=ZkRepro` |\n| 调整 `jute.maxbuffer`（客户端侧） | [build-and-validation.md](build-and-validation.md)、[jute-maxbuffer-limit.md](jute-maxbuffer-limit.md) | `pom.xml` | `exec-maven-plugin`、`systemProperties`、`jute.maxbuffer` | 无测试目录 | `mvn -q exec:java -Dexec.mainClass=ZkRepro`（只有这条路径会带上该属性） |\n| 调整 `jute.maxbuffer`（服务端侧） | [zookeeper-environment.md](zookeeper-environment.md)、[jute-maxbuffer-limit.md](jute-maxbuffer-limit.md) | `env/zookeeper_env.tar.gz` 内 `conf/java.env` | `JVMFLAGS`、`zkEnv.sh`、`zkServer.sh` | 无测试目录 | `tar -xzOf env/zookeeper_env.tar.gz apache-zookeeper-3.8.4-bin/conf/java.env` |\n| 重建 / 替换打包的 ZooKeeper 环境 | [zookeeper-environment.md](zookeeper-environment.md) | `env/zookeeper_env.tar.gz`、`.gitignore` | `conf/zoo.cfg`、`conf/java.env`、`bin/zkServer.sh` | 无测试目录 | `tar -tzf env/zookeeper_env.tar.gz \\| head`，随后启动并跑 demo |\n| 改构建配置（JDK 目标、依赖版本、插件） | [build-and-validation.md](build-and-validation.md)、[zookeeper-environment.md](zookeeper-environment.md) | `pom.xml` | `maven.compiler.source/target`、`org.apache.zookeeper:zookeeper:3.8.4` | 无测试目录 | `mvn -q clean package` |\n| 引入自动化测试 / CI | [build-and-validation.md](build-and-validation.md)、[zk-repro-client.md](zk-repro-client.md) | `pom.xml`、`src/main/java/ZkRepro.java` | `ZkRepro.main` 的 catch 分支（当前吞掉异常） | 需先建立测试骨架 | 先按 [build-and-validation.md](build-and-validation.md) 的检查清单改造退出码，再加测试框架 |\n\n## 先读这几条不变量\n\n1. **两侧都要设置**：`jute.maxbuffer` 必须同时作用于服务端 JVM 与客户端 JVM，只改一侧仍会失败（`README.md` 明确写成 \"on **both** the server and the client\"）。细节见 [jute-maxbuffer-limit.md](jute-maxbuffer-limit.md)。\n2. **服务端取值只在 tarball 内**：解包目录被 `.gitignore` 忽略，直接改解包出来的 `conf/java.env` 不会入库。见 [zookeeper-environment.md](zookeeper-environment.md)。\n3. **客户端取值只对 `mvn exec:java` 生效**：`pom.xml` 的属性注入挂在 exec 插件上，`java -cp` / `java -jar` 需自己加 `-Djute.maxbuffer=2097152`。见 [build-and-validation.md](build-and-validation.md)。\n4. **服务端必须先启动**：客户端 `connectedSignal.await()` 没有超时，服务端未就绪时进程会永久挂起而不是报错。见 [zk-repro-client.md](zk-repro-client.md)。\n5. **退出码不可用于判断成功**：写入失败时异常被捕获后仅打印，进程仍返回 0。见 [zk-repro-client.md](zk-repro-client.md)。\n\n## 端到端验证（需要 Maven 与 JDK）\n\n```bash\ntar -zxf env/zookeeper_env.tar.gz\nmkdir -p zookeeper_data\n./apache-zookeeper-3.8.4-bin/bin/zkServer.sh start      # 服务端带上 conf/java.env 的 JVMFLAGS\nmvn -q clean package\nmvn -q exec:java -Dexec.mainClass=ZkRepro               # 期望输出：Success! (Expected with fix)\n./apache-zookeeper-3.8.4-bin/bin/zkServer.sh stop\n```\n\n前提条件与分层说明见 [build-and-validation.md](build-and-validation.md)：本开发镜像中没有 `mvn`（`java -version` 为 openjdk 23，而 `pom.xml` 目标为 1.8），因此端到端步骤需在装有 Maven 的环境执行；只做编译检查时用 `mvn -q clean package` 即可。\n\n## 文档与实现的一致性提示\n\n- `README.md` 声称修复\"已经应用\"到 `env/zookeeper_env.tar.gz` 与 `pom.xml`：已核对，包内 `conf/java.env` 与 `pom.xml` 插件配置均与 README 一致。\n- `AGENTS.md` 提到\"定时 GitHub Actions 工作流刷新仓库 wiki\"，但仓库中没有 `.github/` 目录，也没有任何 workflow 文件。按此提示查阅存档信息时请以仓库实际内容为准。\n\n## Backlog\n\n- **缺少自动化测试与 CI**：仓库无测试目录、`pom.xml` 无测试依赖与 surefire 配置、无 `.github/`（源锚点：`pom.xml`、`src/main/java/ZkRepro.java`）。原因：`jute.maxbuffer` 行为只能靠手工端到端运行验证，回归风险无法自动拦截。\n- **Java 8 目标在更高 JDK 下的编译结果未实测**：`pom.xml` 设 `maven.compiler.source/target=1.8`，当前环境只有 openjdk 23（源锚点：`pom.xml`、`java -version`）。原因：本轮无 Maven 可用，未取得编译证据。\n","size":5110,"links":["jute-maxbuffer-limit","zk-repro-client","zookeeper-environment","build-and-validation"],"backlinks":["build-and-validation","index"]},{"id":"zk-repro-client","title":"ZkRepro 复现客户端","type":"Component","description":"src/main/java/ZkRepro.java 单类演示程序的职责、关键符号、连接与写入流程、阻塞与退出码等不变量陷阱，以及改动负载/目标集群/失败检测的变更配方。","tags":["zookeeper","java","repro-client","lifecycle","maven"],"body":"\n# ZkRepro 复现客户端\n\n## 定位\n\n`src/main/java/ZkRepro.java` 是本仓库唯一的可执行代码：一个单类（默认包，无 `package` 声明，因此主类名就是 `ZkRepro`）、单入口 `main(String[])` 的顺序式演示程序。它不是库，没有对外 API；运行时依赖由 `pom.xml` 提供（见 [build-and-validation.md](build-and-validation.md)），对端是需要先启动的本地 ZooKeeper 服务端（见 [zookeeper-environment.md](zookeeper-environment.md)）。它要验证的规则本身记录在 [jute-maxbuffer-limit.md](jute-maxbuffer-limit.md)。\n\n历史背景：仓库最初用 Python 脚本 `repro_zk_1m.py` 复现同一问题，随后被这个 Java + Maven 版本整体取代；该脚本已从工作树移除（可用 `git log --diff-filter=D --summary` 查看），因此源码树中除了本类不应再出现其它复现入口。\n\n## 关键符号\n\n| 符号 | 作用 |\n| --- | --- |\n| `ZkRepro.main(String[])` | 唯一入口；按\"连接 → 构造负载 → 清理旧节点 → 写入 → 关闭\"顺序执行。**忽略 `args`** |\n| `connectedSignal`（`CountDownLatch(1)`） | 连接同步原语；只有 Watcher 收到 `SyncConnected` 才放行 |\n| 匿名 `Watcher`（编译为 `ZkRepro$1`） | 只处理 `KeeperState.SyncConnected` 一种状态 |\n| `new ZooKeeper(\"127.0.0.1:2181\", 3000, watcher)` | 连接串与会话超时硬编码 |\n| `sizeInBytes = 1024 * 1024 + 100` | 负载 1048676 字节，刻意略微超过默认 1MB 上限 |\n| `bigData` | 以字符 `'a'` 填充的字节数组 |\n| `nodePath = \"/big_data_node\"` | 目标 znode 路径 |\n| `zk.exists(nodePath, false)` / `zk.delete(nodePath, -1)` | 幂等前置清理，`-1` 表示任意版本 |\n| `zk.create(nodePath, bigData, ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT)` | 写入动作本身 |\n| `zk.close()`（`finally`） | 无论成功或异常都释放会话 |\n\n## 运行流程\n\n```mermaid\nsequenceDiagram\n  participant M as ZkRepro main\n  participant W as 匿名 Watcher\n  participant ZK as ZooKeeper 客户端会话\n  participant S as ZooKeeper 服务端 2181\n  M->>ZK: new ZooKeeper 连接串 127.0.0.1 端口 2181 会话超时 3000\n  S-->>W: SyncConnected 事件\n  W->>M: connectedSignal countDown\n  M->>M: await 解除阻塞 打印 Connected\n  M->>M: 构造 1048676 字节负载\n  M->>ZK: exists /big_data_node\n  ZK->>S: 读请求\n  S-->>M: Stat 或 null\n  alt 节点已存在\n    M->>ZK: delete /big_data_node 版本任意\n  end\n  M->>ZK: create /big_data_node 持久节点 数据为负载\n  S-->>M: 创建成功 或 关闭连接\n  M->>M: 打印 Success 或 Caught unexpected exception\n  M->>ZK: close\n```\n\n图示说明：连接完成由 Watcher 回调驱动，写入路径只有一个 `create`，成功与失败只体现为打印文本。\n\n## 不变量与陷阱\n\n| 可观察行为 | 影响 | 证据 |\n| --- | --- | --- |\n| `connectedSignal.await()` 未设超时 | 服务端未启动或端口不可达时进程永久挂起（不抛异常、不退出），自动化场景必须外部加超时 | `main` 的连接段 |\n| 异常在 `catch (Exception e)` 中仅打印并 `printStackTrace()` | **进程退出码仍为 0**，不能用退出码判断成功；只能匹配标准输出文本 | `catch` 段与 `finally` 段 |\n| `finally` 中调用 `zk.close()` | 失败路径也会关闭会话，不会留下悬挂连接 | `finally` 段 |\n| 先 `exists`/`delete` 再 `create` | 程序可重复运行；`delete` 用 `-1` 避免上次残留节点造成版本冲突 | `main` 写入段 |\n| `CreateMode.PERSISTENT` + `ZooDefs.Ids.OPEN_ACL_UNSAFE` | 节点会留在服务端，且任意客户端可读写；仅适用于本地演示，不要照搬到生产集群 | `zk.create(...)` 参数 |\n| 负载大小与上限的关系 | 负载必须超过 1MB 才有复现意义；若两侧 `jute.maxbuffer` 未同时设置，写入会失败 | 见 [jute-maxbuffer-limit.md](jute-maxbuffer-limit.md) |\n\n## 变更配方\n\n### 配方 A：改变写入负载大小\n\n1. 改 `src/main/java/ZkRepro.java` 中的 `sizeInBytes`（以及 `bigData` 填充逻辑，若需要非重复字节）。\n2. 若新负载序列化后超过两侧现有的 2MB 上限，必须**同步**调大服务端与客户端的 `jute.maxbuffer`：[zookeeper-environment.md](zookeeper-environment.md)（服务端）、[build-and-validation.md](build-and-validation.md)（客户端）。\n3. 同步 `README.md` 的 \"Expected Output\" 中的字节数，避免文档与实现不一致。\n4. 验证：`mvn -q clean package` 保证编译；服务端在运行时再执行 `mvn -q exec:java -Dexec.mainClass=ZkRepro`，观察文本输出。\n5. 非目标：无需改 `.gitignore` 或依赖；无需重建 tarball（除非上限也要变）。\n\n### 配方 B：指向其它 ZooKeeper 集群 / 端口 / 会话超时\n\n1. 改 `new ZooKeeper(\"127.0.0.1:2181\", 3000, …)` 的连接串与超时；`main` 目前完全忽略命令行参数，如需参数化要自行引入（例如读取系统属性）。\n2. 会话超时过小会导致 `SyncConnected` 迟迟不到、程序在 `await()` 处等待更久；调小前先确认网络往返。\n3. 若改的是本地自建服务端的端口，`env/zookeeper_env.tar.gz` 内 `conf/zoo.cfg` 的 `clientPort=2181` 也要同步，见 [zookeeper-environment.md](zookeeper-environment.md)。\n4. 验证：先确认对端可达（本地服务端用 `bin/zkServer.sh status`），再跑 `mvn -q exec:java -Dexec.mainClass=ZkRepro`。\n\n### 配方 C：让失败可被自动化检测\n\n1. 现状：失败被 `catch` 吞掉，退出码恒为 0（见上文不变量表）。\n2. 改造点：在 `catch` 分支改为抛出异常或 `System.exit(非零)`；同时给 `connectedSignal.await()` 加超时并把超时视为失败。\n3. 之后才能引入测试框架（当前 `pom.xml` 无测试依赖、无 surefire 配置），分层与检查清单见 [build-and-validation.md](build-and-validation.md)。\n\n## 聚焦测试与验证\n\n仓库没有测试目录、没有测试依赖，**不存在**针对该类的自动化测试；`ZkRepro.main` 的行为验证只有手工运行一条路径。\n\n| 场景 | 期望观察 |\n| --- | --- |\n| 服务端未启动 | 无任何输出，进程停在 `await()`，只能靠外部超时终止 |\n| 服务端已启动、两侧 maxbuffer 均已设置 | `Connected.` → `Attempting to write 1048676 bytes to /big_data_node...` → `Success! (Expected with fix)` |\n| 服务端已启动、任一侧未设置 maxbuffer | `Caught unexpected exception: …ConnectionLossException…` 加堆栈，退出码仍为 0 |\n\n改动后最小检查：`mvn -q clean package`（编译与打包，静默成功日志）。\n","size":4201,"links":["build-and-validation","zookeeper-environment","jute-maxbuffer-limit"],"backlinks":["build-and-validation","index","jute-maxbuffer-limit","quickstart","zookeeper-environment"]},{"id":"zookeeper-environment","title":"打包的 ZooKeeper 服务端环境","type":"Playbook","description":"env/zookeeper_env.tar.gz 的内容清单、服务端 jute.maxbuffer 的生效链路、启动停止与数据目录/端口约定、.gitignore 契约，以及重新打包服务端配置的安全步骤。","tags":["zookeeper","operations","configuration","environment","packaging"],"body":"\n# 打包的 ZooKeeper 服务端环境\n\n## 用途与内容\n\n`env/zookeeper_env.tar.gz`（约 14MB，1664 个归档条目）是一个预配置好的 Apache ZooKeeper 3.8.4 发行包，用来让复现端到端可运行，而无需自己下载和配置服务端。它承担 [jute-maxbuffer-limit.md](jute-maxbuffer-limit.md) 中\"服务端侧\"的那一半修复。\n\n| 包内路径 | 内容 | 与本仓库的关系 |\n| --- | --- | --- |\n| `apache-zookeeper-3.8.4-bin/conf/java.env` | `export JVMFLAGS=\"-Djute.maxbuffer=2097152\"` | **服务端侧修复的唯一载体** |\n| `apache-zookeeper-3.8.4-bin/conf/zoo.cfg` | `tickTime=2000`、`dataDir=./zookeeper_data`、`clientPort=2181` | 决定数据目录与监听端口 |\n| `apache-zookeeper-3.8.4-bin/bin/zkEnv.sh` | 环境准备脚本 | 若存在 `conf/java.env` 就 source 它 |\n| `apache-zookeeper-3.8.4-bin/bin/zkServer.sh` | `start` / `status` / `stop` | 后台启动，把 `$JVMFLAGS` 传给 JVM |\n| `apache-zookeeper-3.8.4-bin/logs/zookeeper-jules-server-devbox.out` | 打包时残留的上一次运行日志 | 可据其确认曾监听 2181、并起了 AdminServer |\n| `apache-zookeeper-3.8.4-bin/lib/*.jar` | 含 `zookeeper-3.8.4.jar`、`zookeeper-jute-3.8.4.jar` 等 | 手工编译/运行客户端时的 classpath 来源 |\n| `apache-zookeeper-3.8.4-bin/docs/`、`README.md`、`LICENSE.txt` | 上游发行包自带内容 | 与仓库逻辑无关，只占体积 |\n\n## 服务端配置如何生效\n\n1. `bin/zkServer.sh` 会先调用 `bin/zkEnv.sh`。\n2. `bin/zkEnv.sh` 检测到 `$ZOOCFGDIR/java.env` 存在就执行 `. \"$ZOOCFGDIR/java.env\"`，把 `JVMFLAGS` 放进环境。\n3. `bin/zkServer.sh` 启动 JVM 时把它拼进命令行：`java … -cp \"$CLASSPATH\" $JVMFLAGS $ZOOMAIN \"$ZOOCFG\"`。\n\n因此服务端上限**写在这个归档包的 `conf/java.env` 里，而不是 `pom.xml`**；`pom.xml` 只负责客户端侧（见 [build-and-validation.md](build-and-validation.md)），两侧必须同时生效才能写入超过 1MB 的数据（见 [jute-maxbuffer-limit.md](jute-maxbuffer-limit.md)）。\n\n## 启动与停止\n\n```bash\ntar -zxf env/zookeeper_env.tar.gz\nmkdir -p zookeeper_data\n./apache-zookeeper-3.8.4-bin/bin/zkServer.sh start\n./apache-zookeeper-3.8.4-bin/bin/zkServer.sh status\n./apache-zookeeper-3.8.4-bin/bin/zkServer.sh stop\n```\n\n顺序很重要：`ZkRepro` 的连接等待没有超时，服务端未就绪时客户端进程会挂住而不是报错，见 [zk-repro-client.md](zk-repro-client.md)；完整端到端步骤与验证分层见 [build-and-validation.md](build-and-validation.md)。\n\n## 目录与端口约定\n\n- `dataDir=./zookeeper_data` 是**相对路径**，相对启动服务端时的工作目录解析。`README.md` 要求从仓库根目录执行，因此快照落在 `<repo>/zookeeper_data/version-2/`；包内日志里可以看到 `Snapshotting: 0x0 to ./zookeeper_data/version-2/snapshot.0`。\n- `clientPort=2181` 必须与 `ZkRepro` 硬编码的连接串一致。\n- 包内日志显示服务端除 2181 外还启动了 Jetty AdminServer 并绑定 `0.0.0.0:8080`：该端口被占用时服务端启动会受影响，排查启动失败时先看这两处。\n- 后台进程的标准输出落点是 `logs/zookeeper-$USER-server-$HOSTNAME.out`（由 `zkServer.sh` 里的 `nohup … > \"$_ZOO_DAEMON_OUT\"` 决定）；归档包里已有的那份就是上一次运行的残留，**不要把它当成当前会话日志**。\n\n## 与 .gitignore 的契约（容易踩）\n\n`.gitignore` 只有 5 行，全部与本环境有关：`apache-zookeeper-3.8.4-bin/`、`*.tar.gz`、`!env/zookeeper_env.tar.gz`、`zookeeper_data/`、`target/`。\n\n由此推出的三条硬约束：\n\n1. 解包出来的 `apache-zookeeper-3.8.4-bin/` 与运行期数据目录 `zookeeper_data/`、构建产物 `target/` 都不入版本控制。\n2. 服务端配置的**唯一入库副本是 `env/zookeeper_env.tar.gz`**；`*.tar.gz` 的忽略规则被 `!env/zookeeper_env.tar.gz` 显式豁免，这条负向规则是引入归档包时同步添加的（可用 `git log -p -- .gitignore` 查看）。\n3. 因此\"修改服务端配置\"必须改 tarball 内容，直接编辑解包目录是无效修改。\n\n## 变更配方：修改服务端配置（例如把上限改成其它值）\n\n1. 解包：`tar -zxf env/zookeeper_env.tar.gz`（得到仓库根下的 `apache-zookeeper-3.8.4-bin/`）。\n2. 编辑 `apache-zookeeper-3.8.4-bin/conf/java.env`（或 `zoo.cfg`）。\n3. 清理运行残留：删除 `apache-zookeeper-3.8.4-bin/logs/*.out` 以及 `zookeeper_data/`，避免把上一次运行的日志/快照打进新包。\n4. 重新打包（保持顶层目录名不变）：`tar -zcf env/zookeeper_env.tar.gz apache-zookeeper-3.8.4-bin/`。\n5. 只读验证包内内容：`tar -xzOf env/zookeeper_env.tar.gz apache-zookeeper-3.8.4-bin/conf/java.env`。\n6. 若改的是上限，客户端侧要同步（见 [build-and-validation.md](build-and-validation.md)），并跑一次端到端验证（见 [jute-maxbuffer-limit.md](jute-maxbuffer-limit.md)）。\n\n非目标：不需要改 `.gitignore`（豁免规则已存在），也不需要在改 `zoo.cfg` 时动 `pom.xml`；除非客户端侧的上限或连接串也要跟着变。\n\n版本一致性：包内 `lib/zookeeper-3.8.4.jar`、`lib/zookeeper-jute-3.8.4.jar` 与 `pom.xml` 的依赖版本都是 **3.8.4**；升级 ZK 版本时两处必须同步替换，否则客户端与服务端版本不一致（见 [build-and-validation.md](build-and-validation.md)）。\n\n## 只读检查命令\n\n```bash\ntar -tzf env/zookeeper_env.tar.gz | head            # 确认顶层目录结构与版本\ntar -xzOf env/zookeeper_env.tar.gz apache-zookeeper-3.8.4-bin/conf/zoo.cfg\ntar -xzOf env/zookeeper_env.tar.gz apache-zookeeper-3.8.4-bin/conf/java.env\n```\n","size":3900,"links":["jute-maxbuffer-limit","build-and-validation","zk-repro-client"],"backlinks":["build-and-validation","index","jute-maxbuffer-limit","quickstart","zk-repro-client"]}],"edges":[{"source":"build-and-validation","target":"zk-repro-client"},{"source":"build-and-validation","target":"jute-maxbuffer-limit"},{"source":"build-and-validation","target":"zookeeper-environment"},{"source":"build-and-validation","target":"quickstart"},{"source":"index","target":"build-and-validation"},{"source":"index","target":"jute-maxbuffer-limit"},{"source":"index","target":"quickstart"},{"source":"index","target":"zk-repro-client"},{"source":"index","target":"zookeeper-environment"},{"source":"jute-maxbuffer-limit","target":"zookeeper-environment"},{"source":"jute-maxbuffer-limit","target":"build-and-validation"},{"source":"jute-maxbuffer-limit","target":"zk-repro-client"},{"source":"quickstart","target":"jute-maxbuffer-limit"},{"source":"quickstart","target":"zk-repro-client"},{"source":"quickstart","target":"zookeeper-environment"},{"source":"quickstart","target":"build-and-validation"},{"source":"zk-repro-client","target":"build-and-validation"},{"source":"zk-repro-client","target":"zookeeper-environment"},{"source":"zk-repro-client","target":"jute-maxbuffer-limit"},{"source":"zookeeper-environment","target":"jute-maxbuffer-limit"},{"source":"zookeeper-environment","target":"build-and-validation"},{"source":"zookeeper-environment","target":"zk-repro-client"}]}